package com.zplus.adminpanel.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ServiceConfig {
    // Configuration removed - using @Service annotation on implementations instead
    // This avoids duplicate bean creation and circular dependency issues
}
